import { StyleSheet } from 'react-native';

export const globalstyles = StyleSheet.create({
    flatList: {
        height: 10,
        width: '100%'
    },
    flatListContent: {
        flexGrow: 1,
        justifyContent: 'center'
    },
    text: {
        justifyContent: 'center',
        display: 'flex'
      }
});
